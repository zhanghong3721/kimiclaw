# 2026-04-13

## OpenClaw Gateway 配置（Kimi / Moonshot）

- **Kimi Coding API** (`https://api.kimi.com/coding`) 对客户端做了白名单限制，OpenClaw 不在允许列表内，返回 `access_terminated_error`。无法使用。
- **可用方案**：改用标准 **Moonshot API** (`https://api.moonshot.cn/v1`)，provider `api` 字段必须填 `"openai-completions"`（OpenClaw 允许的枚举值之一）。
- **有效 Key**：`sk-zQ32N7dhJKuUpSodZwAvLjUfze1t9pvjWtTK7OC6WYZzzWr8`（在 `api.moonshot.cn/v1` 验证通过）。
- **智谱 fallback Key**：`0ead4d69789e4ccdb7560d33b2314ec5.KU3Tajy5OVQ1g6ry`
- **当前默认模型**：主模型 `moonshot/kimi-k2.5`，fallback `zai/glm-5-turbo`。
- **注意**：`baseUrl` 末尾不能带空格；修改 `~/.openclaw/openclaw.json` 后必须执行 `openclaw gateway restart` 才能生效。

## 中矿资源 2025 年报关键数据

- 津巴布韦 Bikita 矿山锂辉石浮选厂（200 万吨/年）年产约 **30 万吨锂辉石精矿**（Li2O ≥ 7.0%）。
- 透锂长石重选厂（200 万吨/年）年产约 **30 万吨化学级透锂长石精矿**。
- 截至 2025 年底，Bikita 矿区保有锂矿石资源量 12,409.39 万吨，Li2O 平均品位 0.95%，折合 LCE 292.40 万吨。

